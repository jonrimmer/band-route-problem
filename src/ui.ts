export const pointsSvg = (document.getElementById(
  'points'
) as unknown) as SVGSVGElement;
export const routeSvg = (document.getElementById(
  'route'
) as unknown) as SVGGElement;
export const bestCost = document.getElementById('bestCost') as HTMLSpanElement;

export const selectData = document.getElementById('data') as HTMLSelectElement;
